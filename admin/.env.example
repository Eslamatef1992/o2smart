# Copy to .env and fill in for your environment.
# Local dev: leave as /api (proxied to localhost:4000 by vite.config.js)
# Production: the full backend URL, e.g. https://back.o2smart.online
VITE_API_BASE_URL=/api
